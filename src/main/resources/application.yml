spring:
  template:
    cache: false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true

logging:
  level:
    root: info

api:
  credito-api:
    url: https://imersao-credito-api.herokuapp.com